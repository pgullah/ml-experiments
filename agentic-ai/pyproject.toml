[project]
name = "agentic-ai"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[tool.uv.workspace]
members = [
    "ai-travel-agent",
]
